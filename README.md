# Vagrant project to build a VM based on Debian Bookworm64 for software development

## Prerequisite

* 

* From a Windows console:
```sh
> vagrant plugin install vagrant-vbguest
```

* Connection to VM as `vagrant` user:
```sh
> vagrant ssh
```
